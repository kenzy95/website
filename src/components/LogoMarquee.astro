---
/**
 * Marquee infini en CSS pur (zero JS).
 * 10 vrais logos clients reels recuperes du Framer.
 */

interface Logo {
  name: string;
  src: string;
}

/**
 * Tous les logos partagent le meme slot (170x110px). Chaque logo est centre
 * dedans avec object-fit contain + max-height/max-width identiques -> harmonie
 * visuelle parfaite quelles que soient les proportions natives des PNGs.
 */
const clients: Logo[] = [
  { name: 'Trimble', src: '/clients/trimble.png' },
  { name: 'Yomoni', src: '/clients/yomoni.png' },
  { name: 'Solunea', src: '/clients/solunea.png' },
  { name: 'Job2bedone partners', src: '/clients/job2bedone.png' },
  { name: 'Dunforce', src: '/clients/dunforce.svg' },
  { name: 'MOFFI', src: '/clients/moffi.png' },
  { name: 'Dr.Rubber', src: '/clients/dr-rubber.png' },
  { name: 'Les Épaulettes', src: '/clients/epaulettes.png' },
  { name: 'Nestore', src: '/clients/nestore.png' },
];

interface Props {
  title?: string;
  duration?: number;
}
const { title = 'Ils nous font confiance', duration = 40 } = Astro.props as Props;

const logos = [...clients, ...clients];
---

<section class="logo-marquee-section">
  <p class="logo-marquee-eyebrow">{title}</p>

  <div class="logo-marquee-card">
    <div class="marquee-mask">
      <div class="marquee-track" style={`animation-duration:${duration}s`}>
        {
          logos.map((c, i) => (
            <div class="marquee-item" aria-hidden={i >= clients.length ? 'true' : undefined}>
              <img
                src={c.src}
                alt={`Logo ${c.name}`}
                class="marquee-logo"
                loading="lazy"
              />
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  /* Section : conteneur dark, garde l'eyebrow + card blanche centree (pattern agencesimple.fr Framer) */
  .logo-marquee-section {
    padding: 3rem 1.5rem 3.5rem;
    position: relative;
    z-index: 5;
  }
  @media (min-width: 768px) {
    .logo-marquee-section { padding: 3.5rem 2rem 4rem; }
  }

  .logo-marquee-eyebrow {
    text-align: center;
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-creme);
    opacity: 0.55;
    margin-bottom: 2rem;
  }

  /* Card blanche centree, max 1200px, border-radius pill (match Framer 70px) */
  .logo-marquee-card {
    max-width: 1200px;
    margin: 0 auto;
    background: #FFFFFF;
    border-radius: 70px;
    padding: 2.5rem 0;
    overflow: hidden;
  }
  @media (max-width: 640px) {
    .logo-marquee-card {
      border-radius: 32px;
      padding: 2rem 0;
    }
  }

  .marquee-mask {
    overflow: hidden;
    mask-image: linear-gradient(to right, transparent, black 80px, black calc(100% - 80px), transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 80px, black calc(100% - 80px), transparent);
  }
  .marquee-track {
    display: flex;
    width: max-content;
    gap: 5rem;
    align-items: center;
    animation: marquee-scroll linear infinite;
    will-change: transform;
  }
  .marquee-track:hover { animation-play-state: paused; }

  /* Slot uniforme elargi : permet aux wordmarks horizontaux (ratio 4-5) d'atteindre ~100-130px */
  .marquee-item {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 440px;
    height: 180px;
  }
  .marquee-logo {
    max-width: 420px;
    max-height: 140px;
    width: auto;
    height: auto;
    object-fit: contain;
    opacity: 0.85;
    filter: grayscale(20%);
    transition: opacity 200ms ease, filter 200ms ease;
  }
  .marquee-item:hover .marquee-logo {
    opacity: 1;
    filter: grayscale(0%);
  }
  @keyframes marquee-scroll {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }
  @media (prefers-reduced-motion: reduce) {
    .marquee-track { animation: none; }
  }
</style>
