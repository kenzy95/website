---
/**
 * Card "Partenaire WILCO" - vraie banniere SVG du Framer.
 * Le SVG d'origine fait 648x560 avec gradients pink/orange/yellow.
 * Le label "Partenaire" est ajoute en overlay top-left.
 */
---

<section class="wilco-section">
  <div class="mx-auto max-w-[64rem] px-6 flex justify-start">
    <a
      href="https://www.wilco.community"
      target="_blank"
      rel="noopener noreferrer"
      class="wilco-banner"
    >
      <img
        src="/images/wilco-banner.webp"
        alt=""
        class="wilco-banner__img"
        loading="lazy"
        width="1920"
        height="1080"
      />
      <span class="wilco-banner__label">Partenaire</span>
      <span class="wilco-banner__name">WILCO</span>
    </a>
  </div>
</section>

<style>
  /* Section : negative margin top -> card a cheval sur le bas du dashboard */
  /* Encart reduit, leger chevauchement sur le dashboard au-dessus (Kenzy 2026-07-05) */
  /* Colle au coin bas-gauche du dashboard (container 64rem identique) */
  .wilco-section {
    position: relative;
    z-index: 5;
    margin-top: -13rem;
    padding-bottom: 1rem;
  }
  @media (min-width: 768px) {
    .wilco-section {
      margin-top: -21rem;
      padding-bottom: 1.5rem;
    }
  }

  .wilco-banner {
    position: relative;
    display: block;
    width: 100%;
    max-width: 13rem;
    aspect-ratio: 648 / 280;
    border-radius: 24px;
    overflow: hidden;
    isolation: isolate;
    box-shadow:
      0 20px 50px -20px rgba(118, 76, 220, 0.5),
      0 0 0 1px rgba(255, 255, 255, 0.08) inset;
    transition: transform 250ms ease, box-shadow 250ms ease;
  }
  .wilco-banner:hover {
    transform: translateY(-3px);
    box-shadow:
      0 24px 60px -20px rgba(118, 76, 220, 0.65),
      0 0 0 1px rgba(255, 255, 255, 0.12) inset;
  }
  .wilco-banner__img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    z-index: 0;
  }
  .wilco-banner__label {
    position: absolute;
    top: 0.7rem;
    left: 1rem;
    z-index: 2;
    font-family: var(--font-sans);
    font-size: 0.65rem;
    font-weight: 500;
    color: rgba(250, 247, 242, 0.95);
    letter-spacing: -0.01em;
  }
  .wilco-banner__name {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    font-family: var(--font-logo);
    font-weight: 800;
    font-size: 1.5rem;
    color: #FFFFFF;
    letter-spacing: 0.04em;
    text-shadow:
      0 2px 12px rgba(0, 0, 0, 0.3),
      0 0 30px rgba(255, 255, 255, 0.2);
  }
</style>
